import Meta from 'components/ui/Meta';
import Button from 'components/ui/button/Button';
import { FC } from 'react';

const Auth: FC = () => {
  return (
    <Meta title="Authentication">
      <Button theme="yellow">Button</Button>
    </Meta>
  );
};

export default Auth;
